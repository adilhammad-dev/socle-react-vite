import styled from 'styled-components';

// BasicPage styles - currently using container styles
// Add page-specific styles here if needed

export const BasicPageWrapper = styled.div`
  /* Add BasicPage wrapper styles if needed */
`;

