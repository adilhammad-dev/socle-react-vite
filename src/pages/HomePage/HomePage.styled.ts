import styled from 'styled-components';

// HomePage styles - currently using container styles
// Add page-specific styles here if needed

export const HomePageWrapper = styled.div`
  /* Add HomePage wrapper styles if needed */
`;

